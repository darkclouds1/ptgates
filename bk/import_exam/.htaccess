# Disable WordPress rewrite rules for this directory
<IfModule mod_rewrite.c>
    RewriteEngine Off
</IfModule>

# Allow access to PHP files in this directory
<Files "*.php">
    Order allow,deny
    Allow from all
</Files>

# Allow access to CSV files
<Files "*.csv">
    Order allow,deny
    Allow from all
</Files>

